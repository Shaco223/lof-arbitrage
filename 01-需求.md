# 01-需求 / PRD 1.1（LOF 实时溢价监控）

> 版本：PRD 1.1｜状态：定档（评审有条件通过后修订）｜责任人：产品经理(dev-002)｜锁定日期：2026-06-18

## 快速摘要

- 背景：场内 LOF 现有炒股软件展示的溢价基于 T-1 净值，盘中失真 1~2%，吃掉全部可交易信号。
- 核心目标：自建"档 3 + 业绩比较基准补全（工业级）"实时溢价计算与监控系统，目标精度 ≥ 90%。
- 一期范围：仅国内 LOF（QDII 二期）／30 只热门自选池／1 分钟刷新／网页 Dashboard + 阈值推送／30 天分位回看／仅二级市场溢价路径。
- 架构：本机 Python 拉取器（`lof-fetcher/`） + uniCloud 免费云空间（`uniCloud-aliyun/`） + uniapp 前端（`frontend/`，H5 → 小程序）。
- 配套交付：`assets/lof-watchlist-v1.csv`（30 只候选池，含 status 列）、`assets/benchmark-mapping-v1.csv`（基准结构化映射）、`assets/benchmark-validate.md`（权重和自检报告）。
- 远程仓库：https://github.com/Shaco223/lof-arbitrage （主干 `main`，本次修订分支 `feat/prd-v1.1`）。

## 1. 背景与目标

### 1.1 痛点
- 软件溢价 = (现价 − 昨日净值) / 昨日净值，T-1 数据导致盘中失真。
- LOF 可交易套利空间多在 1~3%，与误差同量级，信号几乎不可用。

### 1.2 目标
1. 盘中每分钟输出 30 只 LOF 真实实时溢价率，整体覆盖率 ≥ 90%。
2. 提供 30 天折溢价历史分位，支持判断当前溢价相对位置。
3. 阈值触发时主动推送告警，避免人工盯盘。
4. 前端使用 uniapp 框架，一期 H5，二期可平滑迁移到微信小程序。

## 2. 一期范围与边界

### 2.1 本期包含
- 标的：国内 LOF（普通股票/债券/混合型 LOF），30 只自选池。
- 算法：档 3 + 业绩比较基准补全（详见 §M2）。
- 刷新：盘中（09:30–11:30 / 13:00–15:00）每 1 分钟；盘后 18:00 用官方净值校准。
- 数据源：东方财富 / 新浪 / 天天基金 / 集思录 公开免费接口。
- 历史：30 天折溢价分布 + 当前分位百分比。
- 套利路径：仅二级市场买卖溢价。
- 告警：溢价 > 5% 或折价 < −2%；同基金 30 分钟冷却；推送 Server酱或邮件二选一。
- 前端：uniapp H5。

### 2.2 本期不包含
- QDII LOF、申赎套利、全市场扫描、付费数据源、自动下单、多用户体系。


## 3. 功能模块结构

`
LOF 实时溢价监控（一期）
├─ M1 数据采集（lof-fetcher/，本机 Python，dev-004）
│  ├─ M1.1 行情拉取：LOF 现价 / 重仓股现价 / 指数现价
│  ├─ M1.2 元数据拉取：重仓股权重、业绩比较基准、规模、官方净值
│  ├─ M1.3 调度：apscheduler 盘中每分钟，盘后 18:00 校准
│  └─ M1.4 写入：通过 ingest 接口写 uniCloud（仅此一条写入路径）
├─ M2 实时估值与溢价计算（lof-fetcher/，dev-004）
│  ├─ M2.1 IOPV 估算（档 3 + 基准补全）
│  ├─ M2.2 溢价率计算 = (现价 − IOPV) / IOPV
│  ├─ M2.3 估算覆盖率（按基金类型分流计算，详见 §M2 公式）
│  └─ M2.4 历史溢价 30 天分位（每日校准时刷新）
├─ M3 告警引擎（lof-fetcher/，dev-004）
│  ├─ M3.1 阈值判断（溢价/折价默认 +5% / −2%）
│  ├─ M3.2 冷却控制（同基金 30 分钟）
│  ├─ M3.3 推送通道（Server酱 / 邮件，可配）
│  └─ M3.4 告警日志（alert_log）
├─ M4 后端 API（uniCloud-aliyun/cloudfunctions/，dev-004）
│  ├─ M4.1 api-lof-list           列表 + 实时溢价
│  ├─ M4.2 api-lof-detail         详情 + 30 天分位 + 估算覆盖率
│  ├─ M4.3 api-lof-history        历史 K 线
│  ├─ M4.4 ingest-realtime        拉取器写入（带签名）
│  └─ M4.5 cron-daily-calibrate   每日 18:00 净值校准 + 分位刷新
└─ M5 前端（frontend/，uniapp H5，dev-003）
   ├─ M5.1 Dashboard：30 只 LOF 实时排行（按溢价率降序）
   ├─ M5.2 详情页：曲线 + 分位 + 估算覆盖率三段式标签 + 重仓股
   └─ M5.3 设置页：阈值 / 推送通道
`

### 3.1 开发 Agent 阅读索引

| 角色 | 阅读章节 |
| --- | --- |
| 前端 dev-003 | §3.M5 + §4 + §5 + §6 + §8 + §9.1 / §9.5 / §9.7 |
| 后端 dev-004 | §3.M1–M4 + §M2 公式 + §6 + §7 + §9.1 / §9.2 / §9.4 / §9.5 / §9.6 |
| 测试 dev-005 | §9 全章 + §11 风险 |
| 项目经理 dev-001 | 全文 + §10 决策 + §12 协作 |

## §M2 估算覆盖率公式（**M1 必改修订点**）

### M2-A 覆盖率定义按基金类型分流

设：
- `W_top10` = 前十大重仓股权重之和（来源：季报披露）
- `W_benchmark_assigned` = 已赋指数代码的基准组成项权重之和（不含 CASH）
- `W_cash` = 现金/活期组件权重之和（`index_code = CASH`）

#### 公式 1：指数型 LOF（`type = index`）
> 指数型基金为全样本/抽样复制基准指数，盘中实时净值 ≈ 基准指数实时变动。

`
coverage_index = W_benchmark_assigned + W_cash      # 通常 ≈ 1.00
`

适用条件：`lof_meta.type == 'index'`。
说明：不再使用前十大重仓权重和；前十大重仓股仅用于详情页展示，不进入覆盖率计算。

#### 公式 2：行业型 / 主动型 LOF（`type = industry` / `type = active`）
> 主动管理或行业增强，前十大重仓 + 业绩比较基准对剩余仓位补全。

`
coverage_active = min(1.0, W_top10 + (1 - W_top10) * (W_benchmark_assigned + W_cash))
`

含义：
- 前十大重仓股按精确权重计入。
- 剩余仓位（1 − W_top10）按业绩比较基准结构进行补全；基准中已成功映射到行情源的部分计入覆盖率。
- 任何映射失败的基准组件（`index_code` 找不到行情源）不计入覆盖率，并把该分钟 `source_quality` 标 `degraded`。

适用条件：`lof_meta.type in ('industry', 'active')`。

### M2-B 实现约束

- 后端 M2.1 必须在 IOPV 引擎里硬编码上述两套公式，按 `lof_meta.type` 分支，不在前端二次判断。
- 前端 M5.2 仅消费 `coverage` 字段，不重新计算。
- §8 颜色阈值（≥90% 绿 / 70~90% 黄 / <70% 红）保持不变。

## 4. 页面流程与交互

### 4.1 页面清单
| 页面 | uniapp 路径 | 说明 |
| --- | --- | --- |
| Dashboard | `frontend/pages/index/index` | 30 只 LOF 实时排行 + 排序/筛选 |
| 详情页 | `frontend/pages/detail/[code]` | 单只 LOF 实时溢价 / 30 天曲线 / 分位 / 重仓股 / 三段式覆盖率 |
| 设置页 | `frontend/pages/settings/index` | 阈值、推送通道、自选池开关 |

### 4.2 主流程
1. 用户进入 Dashboard → 调 `api-lof-list` 拉取 30 只 LOF 当前快照。
2. 默认按"溢价率降序"排列，前端每 60 秒轮询刷新一次。
3. 点击行 → 跳详情页，调 `api-lof-detail?code=xxx`，再调 `api-lof-history?code=xxx&days=30`。
4. 详情页顶部固定显示"估算覆盖率"标签（绿/黄/红），点击展开三段式明细（前十大权重 / 基准补全权重 / 现金权重）。
5. 设置页修改阈值后写本地存储 + 通知拉取器配置文件（一期落库为可选）。

### 4.3 关键交互细节
- 列表行字段：代码、名称、现价、IOPV、溢价率、30 天分位、覆盖率。
- 溢价率正值红色、负值绿色，触阈值行加角标。
- 详情图：上半折线（现价 vs IOPV）、下半柱状（溢价率），右侧分位刻度。
- 弱网/超时：前端展示"上次刷新 mm:ss 前"+ 重试按钮。

## 5. 数据字段定义（以 uniCloud 集合为准）

### 5.1 lof_meta（30 只 LOF 元数据）
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | Y | 6 位场内代码 |
| name | string | Y | 基金名称 |
| type | enum | Y | index/industry/active |
| scale_yi | number | Y | 最新规模（亿元） |
| benchmark_raw | string | Y | 业绩比较基准原文 |
| status | enum | Y | active/pending_verify（v1 候选清单状态） |
| coverage_top10 | number | Y | 前十重仓权重和（0~1，仅展示用） |
| updated_at | datetime | Y | 元数据更新时间 |

### 5.2 lof_holdings（前十重仓股）
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | Y | LOF 代码 |
| stock_code | string | Y | 股票代码（带交易所后缀） |
| stock_name | string | Y | 股票名称 |
| weight | number | Y | 权重（0~1） |
| report_date | date | Y | 季报日期 |

### 5.3 lof_realtime（当日分钟级溢价）
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | Y | LOF 代码 |
| ts | datetime | Y | 分钟时间戳 |
| price | number | Y | 二级市场现价 |
| iopv | number | Y | 实时估算净值 |
| premium | number | Y | 溢价率（小数，0.05=5%） |
| coverage | number | Y | 本次估算覆盖率（按 §M2 公式） |
| source_quality | enum | N | ok/degraded/stale |

### 5.4 lof_history（每日收盘溢价，30 天滚动）
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | Y | LOF 代码 |
| date | date | Y | 交易日 |
| close_price | number | Y | 收盘价 |
| official_nav | number | Y | 官方净值 |
| premium_close | number | Y | 收盘真实溢价 |
| premium_pctile_30d | number | Y | 当日溢价在过去 30 天的分位（0~1） |

### 5.5 alert_log（告警发送记录）
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | Y | LOF 代码 |
| ts | datetime | Y | 告警时间 |
| premium | number | Y | 触发时溢价率 |
| direction | enum | Y | premium/discount |
| channel | enum | Y | serverchan/email |
| status | enum | Y | sent/failed/cooldown_blocked |

## 6. API 字段契约（前后端联调基线）

> 基础约定：所有接口走 uniCloud URL 化云函数，`Content-Type: application/json`；统一返回结构。
>
> `{ "code": 0, "message": "ok", "data": { ... } }`；`code != 0` 表示业务错误。
>
> 路径以 uniCloud 云函数名为准，分别对应 `uniCloud-aliyun/cloudfunctions/` 目录下：
> `api-lof-list` / `api-lof-detail` / `api-lof-history` / `ingest-realtime`。

### 6.1 GET `api-lof-list`
- 用途：Dashboard 列表 + 实时溢价快照
- 请求参数：
  | 参数 | 类型 | 必填 | 说明 |
  | --- | --- | --- | --- |
  | sort | enum | N | premium_desc(默认)/premium_asc/code |
  | type | enum | N | all(默认)/index/industry/active |
- 返回 `data`：
  `json
  {
    "ts": "2026-06-18T10:31:00+08:00",
    "items": [
      {
        "code": "161725",
        "name": "招商中证白酒(LOF)A",
        "type": "index",
        "price": 0.823,
        "iopv": 0.805,
        "premium": 0.0224,
        "coverage": 1.00,
        "pctile_30d": 0.82,
        "source_quality": "ok"
      }
    ]
  }
  `

### 6.2 GET `api-lof-detail`
- 用途：详情页头部 + 实时快照
- 请求参数：
  | 参数 | 类型 | 必填 | 说明 |
  | --- | --- | --- | --- |
  | code | string | Y | LOF 代码 |
- 返回 `data`：
  `json
  {
    "code": "161725",
    "name": "招商中证白酒(LOF)A",
    "type": "index",
    "scale_yi": 300,
    "coverage_top10": 0.93,
    "coverage_breakdown": {
      "top10_weight": 0.93,
      "benchmark_assigned_weight": 0.95,
      "cash_weight": 0.05
    },
    "benchmark_raw": "中证白酒指数收益率×95%+银行活期存款利率(税后)×5%",
    "benchmark_components": [
      { "index_code": "399997.SZ", "name": "中证白酒指数", "weight": 0.95 },
      { "index_code": "CASH", "name": "银行活期", "weight": 0.05 }
    ],
    "holdings_top10": [
      { "stock_code": "600519.SH", "stock_name": "贵州茅台", "weight": 0.15 }
    ],
    "realtime": {
      "ts": "2026-06-18T10:31:00+08:00",
      "price": 0.823,
      "iopv": 0.805,
      "premium": 0.0224,
      "coverage": 1.00,
      "source_quality": "ok"
    },
    "pctile_30d": 0.82
  }
  `
- `coverage_breakdown` 三段式由后端按 §M2 公式输出，前端直接渲染（AC-T1）。
  - 指数型：`top10_weight` 字段仍可下发但前端不计入覆盖率展示，仅作"参考重仓"。
  - 行业型/主动型：三段相加按 §M2 公式 2 组合给出 `realtime.coverage`。

### 6.3 GET `api-lof-history`
- 用途：详情页 30 天曲线
- 请求参数：
  | 参数 | 类型 | 必填 | 说明 |
  | --- | --- | --- | --- |
  | code | string | Y | LOF 代码 |
  | days | int | N | 默认 30，最大 60 |
  | granularity | enum | N | day(默认)/minute（minute 仅当日有效） |
- 返回 `data`：
  `json
  {
    "code": "161725",
    "granularity": "day",
    "items": [
      {
        "date": "2026-05-19",
        "close_price": 0.812,
        "official_nav": 0.798,
        "premium_close": 0.0175,
        "premium_pctile_30d": 0.71
      }
    ]
  }
  `

### 6.4 POST `ingest-realtime`
- 用途：拉取器写入（签名）
- Header：`X-Ingest-Token: <UNICLOUD_INGEST_TOKEN>`
- 请求体：
  `json
  {
    "ts": "2026-06-18T10:31:00+08:00",
    "items": [
      {
        "code": "161725",
        "price": 0.823,
        "iopv": 0.805,
        "premium": 0.0224,
        "coverage": 1.00,
        "source_quality": "ok"
      }
    ]
  }
  `
- 返回 `data`：`{ "accepted": 30, "rejected": 0 }`
- 错误码：401 token 错误；400 字段缺失；429 写入超额（uniCloud 配额）。

### 6.5 通用错误码
| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 4001 | 参数缺失或非法 |
| 4010 | 鉴权失败 |
| 4040 | 资源不存在（如 code 不在自选池） |
| 4290 | 写入限流（uniCloud 配额接近上限） |
| 5000 | 服务异常 |


## 7. 30 只 LOF 自选池（v1 候选）

> 文件：`assets/lof-watchlist-v1.csv`（6 列：`code/name/type/scale_yi/benchmark_raw/status`）。
> 筛选原则：规模 ≥ 1 亿元 + 日均成交额 ≥ 500 万元 + 风格分散（指数 / 行业 / 主动 各约 1/3），按规模降序。

### 7.1 status 列约定
- `active`：默认状态，已确认为正常存续 LOF。
- `pending_verify`：分级基金或可能已转型/清盘的标的，待 dev-004 在 M1.2 拉取真实元数据时回推校验。
- 后端 dev-004 在 M1.2 拉真实元数据时若发现失效条目（已转型/清盘 / 规模或成交额不达标），回推 dev-002 → dev-001，由 dev-002 整列替换为 watchlist v2，并由 dev-001 在 main 打 tag `watchlist-v2`。

### 7.2 摘要（前 10 行示例）
| code | name | type | scale_yi | status |
| --- | --- | --- | --- | --- |
| 161725 | 招商中证白酒(LOF)A | 指数 | 300 | active |
| 161005 | 富国天惠成长(LOF)A | 主动 | 180 | active |
| 163406 | 兴全合润(LOF) | 主动 | 150 | active |
| 160706 | 嘉实沪深300(LOF) | 指数 | 80 | active |
| 162411 | 华宝标普石油天然气(LOF)A | 行业 | 40 | active |
| 501050 | 华夏中证500ETF联接(LOF) | 指数 | 40 | active |
| 160505 | 博时主题行业(LOF) | 主动 | 30 | active |
| 162605 | 景顺长城鼎益(LOF) | 主动 | 30 | active |
| 160222 | 国泰国证食品饮料(LOF) | 行业 | 25 | active |
| 161024 | 富国中证军工指数 | 行业 | 18 | pending_verify |

完整 30 行清单见 `assets/lof-watchlist-v1.csv`。当前 `pending_verify` 共 3 只：`161024 / 161121 / 160628`（疑似分级基金）。

### 7.3 业绩比较基准映射
- 文件：`assets/benchmark-mapping-v1.csv`
- 字段：`code, name, component_name, index_code, weight`
- 校验规则：
  1. `sum(weight) per code == 1.000 ± 0.001`
  2. `index_code` 必须能在拉取器索引清单中找到对应行情源；找不到时降级为"使用最大权重指数全替"，并把 `source_quality` 标 `degraded`。
  3. 现金/活期统一标记 `index_code = CASH`。
- 自检报告：`assets/benchmark-validate.md`，30 只 LOF 全部通过 ±0.001 校验。

## 8. 估算覆盖率与降级策略

| 覆盖率 | UI 标签颜色 | 含义 | 降级 |
| --- | --- | --- | --- |
| ≥ 90% | 绿 | 工业级，达标 | 无 |
| 70%~90% | 黄 | 可参考但非高置信 | 前端弹"覆盖率偏低"提示 |
| < 70% | 红 | 不可用作交易信号 | 详情页禁用"加入告警" |

- 详情页头部固定显示"估算覆盖率"标签（数值 + 颜色），点击/hover 弹出三段式明细：
  - 前十大权重 `top10_weight`
  - 基准补全权重 `benchmark_assigned_weight`
  - 现金权重 `cash_weight`
- 当某只 LOF 重仓股或基准指数行情拉取失败时，`source_quality = degraded`；连续两分钟失败标 `stale` 并跳过本分钟写入。

## 9. 验收标准（可执行颗粒度，交付测试 dev-005）

> 每条均给出"测试方法 + 通过条件"，可在测试 Agent 处直接执行。

### 9.1 精度类
- AC-P1：随机抽取 5 只指数型 LOF（含 161725 / 160706 / 501050 / 160119 / 160222），盘后用官方公告净值反推真实溢价；当日每分钟样本 `|实时溢价 − 真实溢价| ≤ 0.5%`。
- AC-P2：30 只自选池整体覆盖率均值 ≥ 90%；个体 `< 70%` 数量 ≤ 3。其中指数型 LOF 覆盖率应 ≈ 1.00（绿）。

### 9.2 完整度类
- AC-C1：盘中 09:30–11:30 / 13:00–15:00 每分钟 `lof_realtime` 应有 30 行数据；允许 ≤ 3 次／日整分钟缺失。
- AC-C2：拉取器单次任务失败可在 30 秒内完成 3 次重试；3 次仍失败写日志并跳过本分钟。

### 9.3 历史与分位
- AC-H1：每只 LOF 详情页可绘制最近 30 天溢价折线，无断点（节假日除外）。
- AC-H2：`premium_pctile_30d` 数值落在 `[0,1]`，与离线脚本计算结果误差 `≤ 0.01`。

### 9.4 告警
- AC-A1：模拟某只 LOF 溢价 > 5% 持续 1 分钟，可在 ≤ 2 分钟内通过配置通道收到一条告警。
- AC-A2：30 分钟内对同一基金多次触发，仅推送 1 次；`alert_log` 应有 `status = cooldown_blocked` 拦截记录。
- AC-A3：盘中告警生效；非交易时段触发应被忽略。

### 9.5 接口
- AC-I1：`api-lof-list` p95 响应 ≤ 800ms；返回 30 行结构合规。
- AC-I2：`api-lof-detail` 包含 `coverage_top10` / `coverage_breakdown` / `benchmark_components` / `holdings_top10` / `realtime` / `pctile_30d` 6 个块。
- AC-I3：`api-lof-history` 在 `days=30` 下返回不少于 20 个交易日。
- AC-I4：`ingest-realtime` 在 token 错误时返回 4010；字段缺失返回 4001；正常写入返回 `accepted == 提交条数`。

### 9.6 架构与配额
- AC-S1：uniCloud 云函数日调用 ≤ 50000、库读 ≤ 50000、库写 ≤ 30000；连续 3 个交易日真实统计达标。
- AC-S2：拉取器写入仅走 `ingest-realtime` 接口；前端不直连拉取器；HBuilderX 可一键编译 H5。
- AC-S3：前端 uniapp 工程在不改源码的前提下，可通过 `npm run build:mp-weixin` 完成微信小程序构建（不要求上线）。

### 9.7 透明度（硬约束落实）
- AC-T1：详情页头部固定显示"估算覆盖率"标签（数值 + 颜色），并在 hover/tap 时弹出三段式明细："前十大权重 + 基准补全权重 + 现金权重"。

## 10. 关键决策记录

| 决策项 | 选择 | 理由 |
| --- | --- | --- |
| 标的范围 | 仅国内 LOF | 聚焦核心问题，控制一期范围 |
| 算法精度 | 档 3 + 业绩比较基准补全 | 工业级 90~95% 精度，工程量可控 |
| 覆盖率口径 | 按基金类型分流（§M2） | 修正 PRD 1.0 指数型误判低覆盖率 bug |
| 刷新频率 | 1 分钟 | 兼顾时效与免费数据源限速 |
| 套利路径 | 仅二级市场溢价 | 一期不做申赎，规避费率/到账复杂度 |
| 推送通道 | Server酱 / 邮件二选一 | 个人用户最低成本 |
| 架构 | 本机 Python + uniCloud + uniapp | 免费额度 + H5 平滑迁小程序 |
| 仓库主干 | `main`（项目经理 squash merge） | 单仓库 + 分支评审 |

## 11. 风险与硬约束

- R1：免费数据源限速 / 字段变更 → 拉取器需指数退避 + 多源切换；元数据每日校准。
- R2：uniCloud 免费额度（云函数 5 万/日 / 库读 5 万 / 库写 3 万）→ 拉取数据放本机，uniCloud 仅做"被前端读 + 接 ingest 写"；批量写入合并 30 只为一次请求。
- R3：重仓股仅披露前十 → 详情页强制三段式覆盖率展示（AC-T1）。
- R4：watchlist v1 含 3 只 `pending_verify` 分级基金，dev-004 真实元数据回填后由 dev-002 固化 v2 整列替换。
- R5：一期不做申赎套利 / 自动下单 / 多用户。

## 12. 协作与版本管理

### 12.1 仓库
- 远程：https://github.com/Shaco223/lof-arbitrage
- 主干：`main`（仅项目经理 dev-001 合并 PRD 评审通过的产物）
- 归档：`wip/legacy-vite-python`（旧 Vite+Vue+Python skeleton，只读）

### 12.2 分支命名
- 格式：`feat/<模块>-<简述>`
- 示例：
  | 角色 | 分支 |
  | --- | --- |
  | 产品资产 | `feat/prd-v1.1` |
  | 前端 | `feat/frontend-uniapp-init` / `feat/frontend-dashboard` |
  | 后端拉取器 | `feat/fetcher-iopv-engine` / `feat/fetcher-alert` |
  | 后端云函数 | `feat/cloud-api-list` / `feat/cloud-ingest` / `feat/cloud-cron-calibrate` |
  | 测试 | `feat/tests-ac-skeleton` |

### 12.3 提交信息
- 规范：Conventional Commits
- 类型：`feat: / fix: / docs: / chore: / refactor: / test:`
- 示例：`docs: 升级 PRD 至 1.1，修复指数型 LOF 覆盖率公式`

### 12.4 流程
1. 开发 Agent 在自己分支上开发 → 推到远程。
2. 完成后回推项目经理 dev-001。
3. dev-001 评审通过后 squash merge 进 `main`。
4. dev-001 在 `main` 打 tag（如 `prd-v1.1` / `watchlist-v2` / `benchmark-v2`）。

### 12.5 节奏
- 每个 `feat/*` 分支应在 1~3 天内合并。
- 超过 3 天未合并的分支必须回报 dev-001 拆分。

### 12.6 资产 tag
- `prd-v1.0` / `prd-v1.1` / `prd-v1.2` ……
- `watchlist-v1` / `watchlist-v2` ……
- `benchmark-v1` / `benchmark-v2` ……
- 仅由项目经理在 squash merge 后打。

### 12.7 并行开发冲突治理（PRD 1.1.1 增补）

> 增补背景：开发阶段存在多 Agent 并行风险（共享文件冲突 / 接口契约冲突 / 重复实现 / 依赖未就绪 / 基线漂移）。本节为 §12 的硬约束补强，**所有 Agent 含项目经理一律适用**。

#### 12.7.1 写权所有制（Single-Writer Map）

每个文件 / 目录有且仅有一个"写权所有者"，其他人只读不写；要改必须发"契约变更请求 (CCR)"或"建议 PR"给所有者。

| 路径 | 写权所有者 |
| --- | --- |
| `01-需求.md` | dev-002 产品经理 |
| `02-技术栈.md` | dev-001 项目经理 |
| `03-开发记录.md` | 全员可写，但必须各自独立追加，走分支 + squash merge 串行化 |
| `04-踩坑记录.md` / `06-复盘.md` | 全员可写，规则同 03 |
| `05-SOP.md` / `README-项目总览.md` | dev-001 项目经理 |
| `assets/lof-watchlist-*.csv` | dev-002 产品经理 |
| `assets/benchmark-mapping-*.csv` | dev-002 产品经理 |
| `assets/benchmark-validate.md` | dev-002 产品经理 |
| `frontend/**` | dev-003 前端 |
| `lof-fetcher/**` | dev-004 后端 |
| `uniCloud-aliyun/**` | dev-004 后端 |
| `tests/**` | dev-005 测试 |
| `outputs/**` | 任意人可写（产物，已 .gitignore） |
| `.gitignore` / `.gitattributes` / `.github/**` | dev-001 项目经理 |

#### 12.7.2 接口契约冻结（Contract Lock）

- §6 API 契约一旦 PRD 定档即 **冻结**。任何字段增删改必须走 **CCR（Contract Change Request）**：
  1. 提出方向 dev-001 发跨会话消息：`【CCR-XX】请求变更 <接口名>.<字段>`，含「现状 / 提议 / 影响范围 / 紧急度」。
  2. dev-001 召开三方对齐（前端 / 后端 / PM）。
  3. 通过则 dev-002 升 PRD（§6 + 变更记录），dev-001 squash merge 并打里程碑 tag（如 `prd-v1.2`）。
  4. dev-001 跨会话广播给前端 / 后端 / 测试，要求 24h 内 rebase。
- 未走 CCR 自创字段者，提交将被项目经理驳回。

#### 12.7.3 Mock 优先并行开发（Mock-First Parallel）

- 前端 dev-003 在后端接口未上线前，必须基于 PRD §6 字段写 **完整 mock**，字段名与契约 100% 一致。
- 后端 dev-004 第一周必须先出 cloudfunctions 空骨架（`return mock`），让前端能联调。
- 后端真实实现完成后，前端 **只删 mock 不改业务代码**。

#### 12.7.4 主干快进（Trunk Sync ≤ 24h）

- 每个 `feat/*` 分支必须 **每天上班第一件事 rebase main**：

```bash
git checkout feat/your-branch
git fetch origin
git rebase origin/main           # 不用 merge，保线性历史
git push --force-with-lease      # 安全 force push
```

- 分叉 main 超过 24h 的分支，dev-001 每日 18:00 巡检后会发提醒；超过 72h 强制要求拆分。

#### 12.7.5 串行合并（Serialized Merge）

- 项目经理 dev-001 是 **唯一合并人**，merge 操作 **串行不并行**。
- 一次只 squash merge 一个分支 → push main → 广播 → 等所有持有 `feat/*` 分支的 Agent 完成 rebase → 才 merge 下一个。
- 大模块（如 `cloud-api-skeleton`）合并前必须有 dev-005 的 Smoke Test 报告。

#### 12.7.6 跨会话广播（Broadcast on Merge）

- 每次 squash merge 完成后，dev-001 必须跨会话广播：

```
【From: 项目经理(dev-001)】【To: 全员】
🔔 main 已更新到 <commit>: <说明>
影响：
- 接口字段变更：…（无 / 列出）
- 公共依赖变更：…（无 / 列出）
请所有持有 feat/* 分支的 Agent 在 24h 内 rebase main。
```

#### 12.7.7 派工指令是单一信号源（Single Source of Tasking）

- **任何 Agent 在收到 dev-001 正式开工指令前，不得创建 `feat/*` 分支或撰写功能代码**。
- 正式指令必须含：阅读范围 / 工作目录 / 技术栈硬约束 / 交付清单 / 分支命名 / 验收 AC 编号 / 时间预期。
- 草稿、讨论、口头承诺均不构成开工依据。
- 违反者由 dev-001 走流程裁决：未提交内容 stash 暂存，已提交内容须补"自检报告"承认违规并冻结合并。


## 变更记录

| 日期 | 变更 | 原因 |
| --- | --- | --- |
| 2026-06-17 | 初始化需求 | 新建项目 |
| 2026-06-18 | 一期需求定档：仅 LOF + 工业级算法 + uniapp/uniCloud | 用户与项目经理讨论后锁定 |
| 2026-06-18 | PRD 1.0：补功能模块 / 页面流程 / 字段 / API 契约 / 可执行验收 | 项目经理 dev-001 派单 |
| 2026-06-18 | PRD 1.1.1：§12.7 增补并行开发冲突治理（写权所有制 / 契约冻结 / Mock 优先 / 24h Rebase / 串行合并 / 广播 / 派工单一信号源） | 项目经理 dev-001 应对多 Agent 并行风险 |
| 2026-06-18 | PRD 1.1：覆盖率公式按基金类型分流；watchlist 去重补 3 只 + status 列；benchmark 自检报告；新增 §3.1 阅读索引、§12 协作与版本管理；目录路径同步方案 A 骨架 | 项目经理 dev-001 评审有条件通过修订意见 |


