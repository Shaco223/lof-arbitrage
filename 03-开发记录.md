# 03-开发记录

## 快速摘要

- 当前阶段：PRD 1.1 定档，准备分发开工
- 最近完成：PRD 1.1 squash merge 进 main 并打 tag prd-v1.1（commit 2aafa96）
- 当前阻塞：无
- 下一步：推 main + tag 到远程，删 feat/prd-v1.1，并行下发 dev-003/004/005 开工

## 最近记录

### 2026-06-17 23:50｜搭建多Agent开发团队（codex-multi-agent-collab）

- 目标：按 codex-multi-agent-collab skill 搭建 LOF 套利项目的五人开发团队，本会话承担项目经理角色。
- 操作：
  1. 阅读 `F:\Codex\.codex\skills\codex-multi-agent-collab\SKILL.md` 与 `examples\dev-team-five-agent-template.md`。
  2. 运行 `scripts\create-team.ps1 -template dev -projectName LOF套利`，生成 5 份角色提示词。
  3. 项目经理角色直接复用本会话，无需新建。
  4. 已为产品经理 / 前端 / 后端 / 测试 4 个会话准备好首条角色提示词，附项目目录与产出位置约束。
- 结果：团队提示词就绪，项目经理角色在本会话激活。
- 验证：`SKILL.md`、模板与脚本均可读取，脚本执行无报错并打印 5 段提示词。
- 下一步：等用户在侧边栏新建 4 个角色会话并回贴会话 ID，再生成身份注册台账并发出“版本定档，全员启动工作”启动语。

### YYYY-MM-DD HH:mm｜项目初始化

- 目标：创建项目文档结构。
- 操作：从全局项目模板复制基础文档。
- 结果：项目上下文可持续沉淀。
- 验证：目录和文档存在。
- 下一步：补充需求和技术栈。


### 2026-06-17 23:55｜身份注册台账（团队上线）

- 目标：登记 5 个 Agent 的会话 ID 与职责边界，作为后续跨会话定向推送的唯一依据。
- 台账：

| AgentID | 角色 | 会话 ID | 岗位职责 | 工作边界 |
| --- | --- | --- | --- | --- |
| dev-001 | 项目经理Agent | 019ed63a-2a47-71e3-9992-a774bc2ddd5c | 整体调度、进度管控、冲突解决、最终汇总 | 只调度，不参与需求/开发/测试 |
| dev-002 | 产品经理Agent | 019ed641-2d62-7892-9ea0-0ec220bdcc32 | 需求分析、PRD 输出、验收标准定义 | 只做产品，不做开发/测试 |
| dev-003 | 前端开发Agent | 019ed641-cbfc-7ef3-b6ad-917de9655eed | 前端代码开发、联调、改 bug | 只做前端 |
| dev-004 | 后端开发Agent | 019ed641-ee79-7d03-b969-a53ddb30d486 | 后端接口与业务实现、数据库设计、改 bug | 只做后端 |
| dev-005 | 测试Agent | 019ed642-10a5-7f03-a9d8-deab9deb0a03 | 测试用例设计、缺陷发现、回归验收 | 只做测试 |

- 消息格式：每条跨会话消息开头必须标注 `【From: <角色名>】【To: <角色名>(<AgentID>)】`，并附完整产出说明。
- 标准开发流程：项目经理 → 产品经理（PRD）→ 项目经理 → 前端 / 后端并行 → 测试 → 整改回环 → 测试通过 → 项目经理汇总通知用户。
- 下一步：推 main + tag 到远程，删 feat/prd-v1.1，并行下发 dev-003/004/005 开工。

### 2026-06-18 00:10｜一期需求定档 + PRD 启动语下发

- 目标：完成与用户的一期需求闭环，正式启动 PRD 阶段，由项目经理(dev-001)向产品经理(dev-002)推送启动语。
- 操作：
  1. 与用户三轮对齐，锁定一期范围：仅国内 LOF / 30 只热门自选 / 档 3 + 业绩比较基准补全（工业级算法）/ 1 分钟刷新 / Dashboard + 阈值推送 / 30 天历史分位 / 二级市场溢价。
  2. 锁定架构：本机 Python 拉取器 + uniCloud 免费云空间（数据库 + 云函数 REST + 校准定时）+ uniapp 前端（H5 → 小程序）。
  3. 锁定告警阈值：溢价 > 5% 或折价 < -2%，同基金 30 分钟冷却。
  4. 项目经理直接落档：把上述决策写入 `01-需求.md`（一期 PRD 输入版）与 `02-技术栈.md`（架构与命令模板），作为产品经理 PRD 的种子。
- 结果：需求与技术栈两份模板已升级为正式版本，决策表与风险表完整登记。
- 验证：`01-需求.md`、`02-技术栈.md` 已写入并可读。
- 下一步：项目经理(dev-001) 向 产品经理(dev-002) 发跨会话启动语，要求基于 `01-需求.md` 输出 PRD 1.0、30 只 LOF 自选池、业绩比较基准映射表。

### 2026-06-18 00:25｜建立 git 版本管理

- 目标：把当前项目状态（含 WIP）纳入 git，建立基线，便于后续协作与回滚。
- 操作：
  1. 新建 `.gitignore`：忽略 `.env`、Python `.venv` / `__pycache__` / `.pytest_cache`、Node `node_modules`、uniapp `unpackage/`、IDE 配置、`outputs/*`（保留 `.gitkeep`）等。
  2. 新建 `.gitattributes`：行尾统一 `LF`，常见二进制类型标 `binary`，避免跨平台/小程序编译问题。
  3. `git init -b main`，配置 `core.quotePath false`、`i18n.commitEncoding=utf-8`、`i18n.logOutputEncoding=utf-8`，避免中文乱码。
  4. 提交首版 commit：`chore: init git repo with project docs and current WIP snapshot`，共 26 个文件、1435 行。
- 结果：仓库基线建立，`main` 分支首 commit 为 `cce739c`。
- 验证：`git log --oneline` 可见首 commit，`git status` 干净。
- 已知问题（下一步处理）：
  - 在协调启动语下发前，前端/后端/测试 Agent 已经各自动手，但走错技术栈：`frontend/` 是 Vite+Vue（应改 uniapp）、`backend/` 是 Python 骨架（应改 uniCloud 云函数）；`assets/` 已有 `lof-watchlist-v1.csv` 和 `benchmark-mapping-v1.csv` 草稿，需要复核。
  - 处理方式：先把 WIP 入仓做基线，再由项目经理统一向各 Agent 下发"按 02-技术栈.md 整改 / 按 PRD 流程走"的纠偏指令，必要时 `git revert` 或新建 `wip/legacy-vite` 分支保留这版供二期参考。
- 下一步：项目经理(dev-001) 向产品经理(dev-002) 推送 PRD 启动语；同时向前端/后端/测试发纠偏指令。

### 2026-06-18 11:30??? Agent ? PRD 1.1 ?9 ? AC ????

- ???? `feat/tests-ac-skeleton` ???????? 17 ? AC ???? + 1 ? e2e ????? PRD ?9?
- ???
  - ?? `tests/_lib/ac_meta.py` ???? AC ????`tests/conftest.py` + `tests/pytest.ini` ?? skip pending ????? `-m ac_hard` ??????
  - ? `tests/ac/` ??? 17 ? `AC-*.test.py`?AC-P1/P2?AC-C1/C2?AC-H1/H2?AC-A1/A2/A3?AC-I1/I2/I3/I4?AC-S1/S2/S3?AC-T1?? hard ????
  - `tests/fixtures/realtime/ac-p1-index-lof-samples.json` ?? 5 ??? LOF?161725/160706/501050/160119/160222?????????
  - `tests/e2e/test_smoke_pipeline.py` ????????`tests/README.md` ?????? + ???? + CI ????
  - ?? `tests/contract/` PRD ?6 ????????? list/detail/history/ingest ?????
  - ???? PRD 1.1 + ?????????? 7 ? Bug ????? `outputs/test-report-20260618.md`??
- ?????? `tests/` ? `python -m pytest -q` ???17 ? AC + 1 ? e2e ? pending skip?9 ????? pass?
- ???`python -m pytest -q` = 9 passed / 18 skipped?`python -m pytest -q contract` = 9 passed?`python -m pytest --collect-only -q -m ac_hard` ?? AC-S1/S3/T1?
- ????? dev-002/dev-004 ?? BUG-001 / BUG-004 ? PRD/?????dev-004 ingest ??????? AC-I4 / AC-C2?dev-003 ??? ready ??? AC-T1?
- ???dev-003 ???? main ? frontend ???????? `frontend/` ?????????

### 2026-06-17 23:30｜按工作台流程创建项目

- 目标：在 F:\CodexWorkspace 中创建“LOF基金套利信息”项目。
- 操作：先读取全局工作台、复利、踩坑、SOP 的低 token 摘要，再使用 `new-project.ps1` 从模板创建项目。
- 结果：项目目录和 7 份标准文档（README-项目总览/01-需求/02-技术栈/03-开发记录/04-踩坑记录/05-SOP/06-复盘）以及 `assets/`、`outputs/` 子目录均已生成。
- 验证：9 项必需文件/目录全部存在。
- 下一步：补充 `01-需求.md`（监控范围、目标 LOF 列表、数据源、刷新频率、套利信号阈值）与 `02-技术栈.md`（数据获取/计算/通知方案）。





### 2026-06-18 11:00｜PRD 1.0 交付（产品经理 dev-002）

- 目标：响应 dev-001 派单，输出 LOF 一期 PRD 1.0 与配套数据资产。
- 操作：
  1. 升级 `01-需求.md` 为 PRD 1.0：补功能模块结构、页面流程、字段定义、4 个 API 字段契约、覆盖率降级策略、可执行验收标准。
  2. 新建 `assets/lof-watchlist-v1.csv`（30 只候选 LOF，5 列）。
  3. 新建 `assets/benchmark-mapping-v1.csv`（业绩比较基准结构化映射，按"组成项"展开）。
- 结果：PRD 1.0 草稿与两份 CSV 已落盘，前后端可据此联调。
- 验证：3 个文件存在；PRD 含 list/detail/history/ingest 4 个接口契约；验收标准均含"测试方法+通过条件"。
- 下一步：dev-001 评审 → 分发 dev-003（前端）/ dev-004（后端）/ dev-005（测试）。





### 2026-06-18 14:00｜PRD 1.1 修订交付（产品经理 dev-002）

- 目标：响应 dev-001 评审反馈，修订 PRD 1.0 的 4 个必改点，输出 PRD 1.1。
- 操作：
  1. 修订 `01-需求.md` 至 PRD 1.1：
     - 新增 §M2 覆盖率公式按基金类型分流（指数型 / 行业-主动型）。
     - 新增 §3.1 开发 Agent 阅读索引。
     - 模块结构 / 页面路径 / API 路径全部同步到方案 A 新目录骨架。
     - 详情页三段式明细落到 AC-T1 与 `coverage_breakdown` 字段。
     - 新增 §12 协作与版本管理（git 分支命名、Conventional Commits、tag 规则）。
  2. 修订 `assets/lof-watchlist-v1.csv`：
     - 删除 3 个重复行（160706 / 160416 / 161116）。
     - 补 3 只活跃 LOF（162605 景顺长城鼎益 / 161226 国投瑞银新兴产业 / 161903 万家行业优选），凑回 30 行。
     - 新增 `status` 列：3 只分级基金嫌疑（161121 / 161024 / 160628）标 `pending_verify`，其余 27 只标 `active`。
  3. 修订 `assets/benchmark-mapping-v1.csv`：同步上述 30 只清单展开组成项；现金/活期统一 `index_code = CASH`。
  4. 新增 `assets/benchmark-validate.md`：脚本生成 30 只 LOF 的 `sum(weight)` 自检清单，全部通过 `±0.001` 校验。
- 结果：PRD 1.1 + 3 份资产已落盘，`feat/prd-v1.1` 分支待提交推送。
- 验证：
  - PRD 含 §M2 双公式 / §3.1 阅读索引 / §12 协作 / coverage_breakdown 字段。
  - watchlist 31 行（含表头），无重复 code+name 组合，30 行数据。
  - benchmark-validate.md 汇总：通过 30 / 不通过 0。
- 下一步：本地 add/commit/push `feat/prd-v1.1` 分支，回推 dev-001 审核 → squash merge + tag `prd-v1.1`。



### 2026-06-18 15:00｜PRD 1.1 评审通过签收（产品经理 dev-002）

- 目标：签收 dev-001 关于 PRD 1.1 的评审结论。
- 操作：本地切回 `main`、`git pull --ff-only` 同步 squash merge `2aafa96` 与 tag `prd-v1.1`；确认 `feat/prd-v1.1` 本地/远程分支已删除。
- 结果：
  - M1 覆盖率公式分流（指数型 / 行业主动型）核验通过。
  - M2 watchlist 30 行 / status active=27 / pending_verify=3 核验通过。
  - M3 benchmark 30/30 通过 ±0.001 校验。
  - M4 §12 协作流核验通过；§3.1 阅读索引与 `coverage_breakdown` 字段加分项到位。
- 验证：`git log` 显示 `2aafa96 feat(prd): 合并 PRD 1.1` 在 main；`git tag --list 'prd-*'` 含 `prd-v1.1`。
- 下一步（产品经理待办）：
  1. 等 dev-004 在 M1.2 拉真实元数据后回推"失效条目清单"。
  2. 在 `feat/watchlist-v2` 分支固化 watchlist v2（整列替换 + 重新跑 benchmark-validate），推送回推 dev-001 走 squash merge + tag `watchlist-v2`。
  3. 接受 dev-003 / dev-004 / dev-005 在开发中提出的需求疑问，必要时升 PRD 1.2。



### 2026-06-18 16:00｜签收流程裁决 #001（产品经理 dev-002）

- 目标：签收 dev-001 关于"产品经理直推 main"事件的流程裁决 #001（方案 A）。
- 操作：
  1. 切到 `main`，`git fetch --prune --tags` 同步远程；确认远程 HEAD = `d966d5b docs(arbitration): 登记流程裁决 #001`。
  2. 核验 `f5f7d30 docs(ack): 产品经理签收 PRD 1.1 评审通过` 已 squash 入 main，原直推提交 `d92d10b` 已搬至 `feat/docs-prd-v1.1-ack` 留证后清理。
  3. 新建 `feat/docs-arbitration-001-ack` 分支登记本次签收，遵循 §12.4 分支 → 回推 → squash merge 流程。
- 结果：本地与远程 main 一致；裁决决议已知悉并写入项目记忆。
- 验证：`git log origin/main --oneline -3` 含 `d966d5b / f5f7d30 / 1a7c8e5`；本地工作树干净（dev-005 未跟踪文件不归本提交处理）。
- 个人结论（自我约束）：
  - §12.4 适用全员含项目经理；任何文档类小动作也必须走分支。
  - 后续所有产品文档变更（PRD / watchlist / benchmark / 开发记录登记）一律先建 `feat/<模块>-<简述>`，提交后回推 dev-001 走 squash merge。
  - 直推 main 事件不再发生。
- 下一步：维持既有待办：
  1. 等 dev-004 M1.2 真实元数据回推"失效条目清单"。
  2. `feat/watchlist-v2` 固化 v2 + 重跑 `benchmark-validate`。
  3. 接受 dev-003 / dev-004 / dev-005 的需求疑问，必要时升 PRD 1.2。



### 2026-06-18 17:00｜签收 PRD 1.1.1 增补与裁决 #002（产品经理 dev-002）

- 目标：签收 dev-001 关于 PRD 1.1.1 §12.7 并行开发冲突治理增补 + 裁决 #002 的同步通知，并把 7 条铁律内化为产品经理的硬约束。
- 操作：
  1. `git fetch origin --prune --tags`，远程 main HEAD = `c00a5ef docs(arbitration): 登记流程裁决 #002 + PRD 1.1.1 落地`；本地 `main` 已 fast-forward。
  2. 阅读 `01-需求.md` §12.7 全部 7 条铁律（写权所有制 / 契约冻结 / Mock 优先 / 24h Rebase / 串行合并 / 广播 / 派工单一信号源）。
  3. 新建 `feat/docs-prd-v1.1.1-ack` 分支登记本次签收，遵循 §12.4。
- 结果：tag `prd-v1.1.1` (`cc67dc7`) 与 `c00a5ef` 已确认，本地远程一致。
- 验证：`git tag --list 'prd-*'` 含 `prd-v1.1` / `prd-v1.1.1`；`git log origin/main --oneline -3` 含 `c00a5ef / cc67dc7 / d655b8a`。
- 产品经理硬约束认领（§12.7 落地清单）：
  - **写权所有制（12.7.1）**：我独占以下文件的写权，他人改动须发 CCR 给我：
    - `01-需求.md`
    - `assets/lof-watchlist-*.csv`
    - `assets/benchmark-mapping-*.csv`
    - `assets/benchmark-validate.md`
  - **接口契约冻结（12.7.2）**：§6 已冻结。任何字段增删改我必须先收到三方 CCR 决议后才升 PRD（`feat/prd-v1.2` 或 `feat/prd-v1.x` 分支）。
  - **派工单一信号源（12.7.7）**：在收到 dev-001 正式派工前，我不主动起新功能分支；仅维护既有交付物 + 响应需求疑问。
  - **跨会话广播协同**：dev-001 每次合并后我会将影响面（接口字段 / 公共依赖）回看一次，确认是否需要同步升 PRD。
- 当前待办（不变）：
  1. 等 dev-004 M1.2 真实元数据回推"失效条目清单"。
  2. 起 `feat/watchlist-v2` 分支整列替换 + 重跑 `benchmark-validate`。
  3. 接受 dev-003 / dev-004 / dev-005 的需求疑问，必要时走 CCR 升 PRD 1.2（`feat/prd-v1.2` → 回推 → squash merge + tag `prd-v1.2`）。


### 2026-06-18 18:00｜流程裁决 #003：多角色工作区隔离

- 背景：dev-002/dev-003/dev-004/dev-005 原计划共用同一个物理目录开发，但 Git 工作区同一时间只能 checkout 一个分支，多个 Codex 会话会互相切换分支并污染工作状态。
- 裁决：采用 Git worktree 隔离。项目根目录仅由项目经理 dev-001 使用；各角色固定进入自己的 `.worktrees/<role>` 目录开发。
- 固定目录：
  - dev-002 产品经理：`.worktrees/dev-002-product`
  - dev-003 前端开发：`.worktrees/dev-003-frontend`
  - dev-004 后端开发：`.worktrees/dev-004-backend`
  - dev-005 测试：`.worktrees/dev-005-test`
- 执行结果：
  1. `.gitignore` 已加入 `.worktrees/`，防止本地 worktree 目录入库。
  2. 已创建四个角色 worktree，并固定绑定对应工作分支。
  3. dev-003 与 dev-005 的既有越位分支已在独立 worktree 中 rebase 到最新 `origin/main`。
  4. 旧 stash 已核验：dev-003/dev-005 的有效工作已在对应分支中；剩余 stash 仅作为迁移留证，禁止角色自行跨目录套用。
- 新纪律：
  - 任何角色禁止在项目根目录执行 `git checkout <自己的分支>`。
  - 任何角色只在自己的 worktree 中 `git fetch` / `git rebase origin/main` / `git push`。
  - dev-001 仍为唯一合并人，合并前从各角色远程分支审核差异并在根目录 squash merge。
  - 如需新增角色或新增长期分支，先由 dev-001 创建 worktree 后再派工。


### 2026-06-18 18:00｜固化 watchlist-v2（产品经理 dev-002）

- 目标：响应 dev-001 正式派工，基于 dev-004 二次元数据验证报告固化一期 watchlist-v2 与 benchmark-v2。
- 操作：
  1. 在 `dev-002-product` worktree 内从 `feat/product-docs` 新建 `feat/watchlist-v2`，并 rebase 到 `origin/main = 050ac6a`。
  2. 新增 `assets/lof-watchlist-v2.csv`：30 只国内 LOF，保留/改名/替换/QDII 后置按 dev-001 裁定原则执行。
  3. 新增 `assets/benchmark-mapping-v2.csv`：随 watchlist-v2 整体重建基准组成项。
  4. 新增 `assets/benchmark-validate-v2.md`：30/30 通过 `sum(weight) per code == 1.000 ± 0.001`。
  5. 更新 `01-需求.md` §7：v2 当前推荐池、处理统计、status 约定、399987 冲突处理；更新 R4 与变更记录。
- 结果：
  - v2 保留 8 只；改名/修 type/status 后保留 4 只；QDII 后置 4 只；替换出池 13 只；人工复核后移出 1 只；新增补位 18 只。
  - v2 status：`active=17`，`active_low_liquidity=13`。
  - 399987 冲突：`160212` 移出，`160632` 保留并统一映射 `399987.SZ`（中证酒指数），v2 不再存在同数字多语义冲突。
- 验证：
  - `assets/lof-watchlist-v2.csv`：30 行数据、无重复 code。
  - `assets/benchmark-mapping-v2.csv`：覆盖 30 只基金。
  - `assets/benchmark-validate-v2.md`：30 只全部 PASS。
  - 未触发 PRD §6 字段契约变更，不需要 CCR / PRD 1.2。
- 下一步：推送 `feat/watchlist-v2` 回推 dev-001 审核；通过后由 dev-001 squash merge 并打 tag `watchlist-v2` / `benchmark-v2`。


### 2026-06-18 19:00｜PRD M2 告警/刷新/历史口径增补（产品经理 dev-002）

- 目标：响应 dev-001 正式派工，在不改 PRD §6 API 字段契约的前提下，补充 M2 观察告警阶段产品口径。
- 操作：
  1. 在 dev-002-product worktree 内新建 `feat/product-m2-watch-alerts` 分支。
  2. 更新 `01-需求.md`，新增 `§M2-B 监控告警、刷新与历史沉淀`。
  3. 明确告警阈值沿用 Settings：溢价 `>= +5%`、折价 `<= -2%`。
  4. 明确 Dashboard 展示：高溢价红色/高亮，高折价绿色/高亮，`source_quality=degraded/stale` 必须提示不可盲用。
  5. 明确刷新策略：真实 API 默认 60 秒轮询，支持手动刷新，非交易时段提示“数据可能滞后”。
  6. 明确历史沉淀：本地优先、线上后置，至少覆盖最近 30 日日线与当日分钟快照扩展口径。
  7. 明确不做：QDII、线上高频联调、AC-S1 放行。
- 结果：M2 小节已补充；PRD §6 未改，不触发 CCR / PRD 1.2。
- 验证：仅修改产品文档与开发记录；未触碰前端/后端/测试目录。
- 下一步：推送 `feat/product-m2-watch-alerts` 回推 dev-001 审核；通过后由 dev-001 squash merge。

### 2026-06-19 00:00｜PRD M3 真实行情采集口径增补（产品经理 dev-002）

- 目标：响应 dev-001 正式派工，在不改 PRD §6 API 字段契约的前提下，补充 M3 真实行情采集闭环产品口径。
- 操作：
  1. 在 `dev-002-product` worktree 内新建 `feat/product-m3-real-quotes` 分支，并基于最新 `origin/main` 开展文档修订。
  2. 更新 `01-需求.md`，新增 `§M3 真实行情采集闭环（POC 先行）`。
  3. 明确 POC 首批 5 只：`161725` / `161005` / `160706` / `160632` / `501203`。
  4. 明确真实行情源：东方财富 `push2` + 新浪备源采集场内价格；天天基金 `fundgz` / 东方财富估值页提供估算净值；指数/重仓行情按免费源优先。
  5. 明确字段口径：M3 必须复用 §6 既有 `price` / `iopv` / `premium` / `ts` / `source_quality` / `coverage` / `coverage_breakdown` 等字段，不新增、不删除、不改名。
  6. 明确降级规则：`ok` / `degraded` / `stale` 三档，单标的失败不影响其他标的，`degraded/stale` 必须提示不可盲用。
  7. 明确验收标准：3~5 只连续采集 ≥10 分钟、有效分钟率 ≥80%、仅走本地 API `http://127.0.0.1:8787`。
- 结果：M3 产品口径已补充；不触发 PRD §6 CCR，不升 PRD 1.2。
- 验证：仅修改 `01-需求.md` 与 `03-开发记录.md`；未触碰前端/后端/测试目录；未引入 token 或线上 uniCloud 高频联调要求。
- 下一步：推送 `feat/product-m3-real-quotes` 回推 dev-001 审核；通过后由 dev-001 squash merge。

### 2026-06-19 11:00｜PRD 1.2 §6 字段对齐升级（产品经理 dev-002）

- 目标：响应 dev-001 PRD 1.2 字段对齐升级派工（方案 B：隐藏假字段 + 补真元数据 + 对齐竞品），把 §6 list/detail 接口扩展到与集思录盘中价值「估算溢价 / 净值溢价 / 误差」三件套对齐，且不动接口名、不破坏旧字段。
- 操作：
  1. 在 `dev-002-product` worktree 内新建 `feat/prd-v1.2-fields-align` 分支，基线 `origin/main = 83d6cef`。
  2. 升级 `01-需求.md` 标题与快速摘要至 PRD 1.2，状态：草稿。
  3. §6.1 list 接口扩展字段：`price_change_pct` / `volume_amount` / `nav_official` / `nav_official_date` / `premium_nav` / `premium_error` / `subscribe_status` / `redeem_status` / `status` / `fund_scale` / `circulating_shares`；旧字段全部保留。
  4. §6.2 detail 接口包含 list 全部字段，并新增 `nav_estimate_error_pct`；`holdings_top10[]` 增加 `price_change_pct`（选填）与 `contribution_pct`（必填，缺值返回 `null`）。
  5. 字段兜底约束写入 §6.1 / §6.2：新字段为 `null`/空时前端"无值则隐藏"，不破坏 `mock-contract` / `real-api-config` 现有校验；旧字段不改名/不删除。
  6. 新增 §4.4 详情页布局对齐竞品（产品稿）：左列「场内」、右列「估值」双列卡片；中部状态标签（申购/赎回/低流动性/数据降级）；下部重仓表 + 估值时间 + 贡献度；历史图保留。
  7. 新增 §9 验收：AC-P3（估算误差 <1% 否则降级）、AC-P4（净值溢价公式误差 ≤1e-4）、AC-P5（字段兜底渲染不报错）。
  8. §2.2 明确不做扩展：不做套利结果自动计算（二期）；不新增 `shares_change_daily`（免费源覆盖差，T+1 不适合盘中决策，需 CCR 升 PRD 1.3）；不动 list/detail/history/ingest 四个接口名；不放行 AC-S1；不打线上 uniCloud。
  9. §10 新增决策行：§6 字段对齐选方案 B；§11 新增 R6：申购/赎回状态/规模/份额数据源由 dev-004 评估，无源时 `null` / `unknown`，前端隐藏不阻塞契约。
  10. 变更记录追加 PRD 1.2 行（日期 2026-06-19）。
- 结果：PRD 1.2 草稿已落，§6 list/detail 字段全部按方案 B 升级；旧字段保留，接口名不动；不放行 AC-S1，不打线上 uniCloud。
- 验证：
  - `git diff --check` 通过；
  - `git diff --name-only` 仅 `01-需求.md` 与 `03-开发记录.md`；
  - 全局检查未删除任何旧字段：`coverage_top10` / `coverage_breakdown` / `benchmark_raw` / `benchmark_components` / `holdings_top10` / `realtime` / `pctile_30d` / `source_quality` / `premium` / `iopv` / `price` 均仍出现。
- 下一步：推送 `feat/prd-v1.2-fields-align` 回推 dev-001 审核；通过后由 dev-001 squash merge + tag `prd-v1.2`，再由 dev-001 串行派工 dev-004 → dev-003。

### 2026-06-22 ｜PRD 1.2.1 误差口径修订（CCR，产品经理 dev-002）

- 背景：6/22 周一真盘 12 次迭代出现 degraded=13 / stale=90，但 dev-004 确认 0 只数据源真失效。根因是现行 AC-P3 用 `premium_error = iopv(实时) - nav_official(T-1 昨日净值)`，盘中大盘一波动 IOPV 自然偏离昨日净值 ≥1%，导致 90% 基金被误判 `stale`，用户满屏「数据滞后，不可盲用」——误报而非真降级。
- 操作：
  1. 在 `dev-002-product` worktree 起 `feat/prd-v1.2.1-error-caliber`，基线 `origin/main = 4f888ff`。
  2. 标题/版本升级为 PRD 1.2.1，锁定日期 2026-06-22。
  3. AC-P3 重定义为「盘后估算准确度校验」：收盘后用当日收盘估算净值 vs 当日官方净值（T+1 公布）；`估算准确度误差 = |当日收盘估算净值 - 当日官方净值| / 当日官方净值`；盘后误差 ≥1% 仅标记「估算偏差较大」（置信度提示），不参与盘中实时降级；新增反例守门：盘中单边波动场景 0 误报。
  4. 盘中 `source_quality` 降级口径改为「仅看数据可得性」：`degraded`=估值取不到/走 T-1 兜底/行情延迟；`stale`=连续两分钟取不到实时价/估值；不再因 `premium_error` 大小触发降级（§M3.5、§8、§6.1 兜底规则同步修订）。
  5. 字段语义澄清（不改字段名/接口名）：`premium_error` / `nav_estimate_error_pct` 语义由「实时误差」改为「盘后估算准确度」（展示历史口径，盘中显示上一交易日参考）；`premium_nav` 保持不变（对标集思录「净值溢价(昨收)」）。
  6. §4.4 右列「估值」误差展示口径同步修订；§10 决策表加「误差口径（PRD 1.2.1）」行；§11 新增 R7 误报风险。
  7. 变更记录追加 2026-06-22 PRD 1.2.1 行。
- 结果：盘中波动与估算准确度彻底分离；盘中降级只看数据可得性，估算准确度走盘后回测。字段名/接口名不变 → 非结构性 CCR，走 PRD 1.2.1 小版本。
- 验证：
  - `git diff --check` 通过；
  - `git diff --name-only` 仅 `01-需求.md` 与 `03-开发记录.md`；
  - `premium_nav` 公式行未被删除/改名；list/detail/history/ingest 接口名零变更。
- 明确不做：不删 `premium_nav`；不改字段名/接口名；不放行 AC-S1。
- 下一步：推送 `feat/prd-v1.2.1-error-caliber` 回推 dev-001 审核；通过后由 dev-001 squash merge + tag `prd-v1.2.1`，再串行派 dev-004（改降级逻辑）+ dev-005（同步 AC-P3 验收）。
